<div class="is-vcentered">
  {{> TitleBar title="All about forests" subTitle="Favorites Forests!"}}
</div>

<div class="columns">
  {{#if placemark }}
  <div class="column has-text-centered">
    <div>{{> DetailsList }}</div>
  </div>
  {{else }}
  <div class="column has-text-centered">
    <img width="300" src="images/sign-post.png" style="border-radius: 50%"/>
  </div>
  {{/if }}
  
  <div class="column box mt-6 has-text-centered">
    <h1 class="title is-4">Start Listing!</h1>
    <div>{{> PlacemarksForm }}</div>
    <div>{{> PlacemarksList }}</div>
  </div>
</div>

